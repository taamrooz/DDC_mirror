#pragma once
#include <vector>
#include <map>
#include <string>
#include "Component.h"

class EntityManager
{
private:
	uint16_t _id = 1;
	std::vector<size_t> _entities;
	std::map<std::string, std::vector<Component>> _componentsByClass;

public:
	int CreateEntity(std::vector<Component> component);
	
};

